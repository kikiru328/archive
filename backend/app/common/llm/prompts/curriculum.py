CURRICULUM_GENERATION_PROMPT = """
목표: {goal}
기간(주): {period}  
난이도: {difficulty}
세부요청: {details}

**중요**: 반드시 주어진 목표({goal})에 맞는 커리큘럼을 생성하세요. 
목표가 애매하더라도 프로그래밍이 아닌 다른 주제일 수 있습니다.

# 규칙
- 목표에 정확히 맞는 실용적이고 구체적인 커리큘럼을 생성.
- 각 주차는 최소 1개, 최대 5개 레슨.
- 모든 텍스트는 한국어.
- 주차 제목 형식: 반드시 **"<핵심 요약>"** 로 시작해야 함 (예: "개발환경 세팅").
- 주차는 1부터 {period}까지 중복 없이 오름차순.

다음 JSON 형식으로 응답:
{{
  "title": "<목표에 맞는 커리큘럼 제목>",
  "schedule": [
    {{ "week_number": 1, "title": <핵심요약>, "lessons": ["레슨1","레슨2"] }},
    {{ "week_number": {period}, "title": <핵심요약>, "lessons": ["레슨1","레슨2"] }}
  ]
}}
"""
